# Simple RAG

Sistema RAG simples usando LangChain, Ollama e ChromaDB.

## Instalação

```bash
pip install -e .
```

## Uso

```bash
chainlit run simple_rag/main.py -w --host='0.0.0.0' --port=9000
```